# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

pod 'Alamofire', '~> 5.2'


target 'LinxShare (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'

  # Pods for LinxShare (iOS)

end


target 'ShareExtension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'

  # Pods for LinxShare (ShareExtension)

end



target 'LinxShare (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'

  # Pods for LinxShare (macOS)

end
